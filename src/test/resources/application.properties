# Test overrides. Observability is disabled so @SpringBootTest integration tests never attempt an
# OTLP export (no LGTM stack runs under Testcontainers) — the agent installs no OpenTelemetry
# feature, proving observability is additive and never required (AC-19c).
app.observability.enabled=false
